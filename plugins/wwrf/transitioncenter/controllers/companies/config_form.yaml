# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Company

# Model Form Field configuration
form: $/wwrf/transitioncenter/models/company/fields.yaml

# Model Class name
modelClass: Wwrf\TransitionCenter\Models\Company

# Default redirect location
defaultRedirect: wwrf/transitioncenter/companies

# Create page
create:
    title: Create Company
    redirect: wwrf/transitioncenter/companies/update/:id
    redirectClose: wwrf/transitioncenter/companies

# Update page
update:
    title: Edit Company
    redirect: wwrf/transitioncenter/companies
    redirectClose: wwrf/transitioncenter/companies

# Preview page
preview:
    title: Preview Company
